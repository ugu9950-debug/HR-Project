# ── 인사 원본 데이터 — 개인정보 포함. 절대 커밋 금지 ──────────────
# 하위 폴더에도 같은 규칙이 있지만, 루트에 두어야 어느 위치에서 실행하든 걸립니다.
*.xlsx
*.xls
*.xlsm
*.csv
~$*

# ── 의존성 · 빌드 부산물 ────────────────────────────────────────
node_modules/
.vercel/
.vercel
.env*
